// Dashboard (summary widget + receipt list). OWNER: Emma + David. EPIC-4, Day 6-8.
export default function DashboardPage() {
  return (
    <main className="mx-auto max-w-3xl p-8">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-sm text-gray-500">Summary + receipt list — TODO(EPIC-4).</p>
    </main>
  );
}
